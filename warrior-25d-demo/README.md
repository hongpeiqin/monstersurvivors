# 2.5D 八方向战士 Demo

在线地址：<https://hongpeiqin.github.io/monstersurvivors/warrior-25d-demo/>

这是一个单页 HTML5 Canvas 战斗演示，包含八方向持刀战士、虚拟摇杆、普通攻击与三个技能、持续刷新的追踪怪物、程序化背景音乐和攻击音效。

本构建新增：

- 设置面板中的粒子特效开关，关闭后立即清空并停止生成附加粒子，选择会持久化保存。
- 怪物寻路与群体避让按 2、3 或 5 个帧批次轮转，移动与攻击判断保持逐帧。
- 空间网格邻居查询，避免怪物每帧进行全量两两遍历。
- 粒子物理按 2、3 或 4 个帧批次轮转，并通过位置外推维持连续运动。
- 自适应轻载、均衡、降载三级调度，以及动态粒子上限、绘制预算和辉光强度。
- 怪物、粒子和附加效果视口裁剪，HUD 按负载降低刷新频率。
- 背景音乐、攻击音效独立音量以及总静音开关。

`index.html` 是 Pages 启动器，`payload/` 中的文本分片是原始单文件 Demo 的 Base64 编码，`manifest.json` 记录构建版本和源码 SHA-256。分片仅用于绕开文件接口上传限制，浏览器载入后执行的内容与独立版 `index.html` 完全一致。

构建：`3721a21-442bc111`  
源码 SHA-256：`442bc111ee6ccf4eb3eaa46c788cd5b6963f17cf53ae8df1ed5ed7fe8caf050b`
